---
import InfoBox from "./InfoBox.astro";

interface Props {
    title?: string;
    content: string;
}

const { title, content } = Astro.props;
---

<InfoBox
    title={title ? title : "Eintritt"}
    icon="/icons/eintritt.svg"
    content={content}
/>
