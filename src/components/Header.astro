---
import HeaderLink from "./HeaderLink.astro";
import { SITE_TITLE } from "../consts";
---

<header>
    <nav>
        <a style="padding:0;border-bottom:0;" href="/"
            ><img src="/Logo.svg" alt="" style="width:9rem;height:9rem;" />
        </a>
        <div class="internal-links">
            <HeaderLink href="/">Home</HeaderLink>
            <HeaderLink href="/travel-diary">Reisetagebuch</HeaderLink>
            <HeaderLink href="/travel-guide">Reisetipps</HeaderLink>
            <HeaderLink href="/about">Über uns</HeaderLink>
        </div>
    </nav>
</header>

<style>
    header {
        margin: 0;
        padding: 0 1em;
        background: white;
        box-shadow: 0 2px 8px rgba(var(--black), 5%);
    }
    h2 {
        margin: 0;
        font-size: 1em;
    }

    h2 a,
    h2 a.active {
        text-decoration: none;
    }
    nav {
        display: flex;
        align-items: center;
        justify-content: space-evenly;
    }
    nav a {
        padding: 1em 1em;
        color: var(--black);
        border-bottom: 4px solid transparent;
        text-decoration: none;
    }
    nav a.active {
        text-decoration: none;
        border-bottom-color: var(--accent);
    }

    @media (max-width: 720px) {
        nav {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
    }
</style>
